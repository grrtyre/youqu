# 📝 Markdown 预览器

> 苹果白风格的本地 Markdown 预览工具：左侧编辑、右侧实时渲染，支持 GFM 语法、中文排版优化、代码高亮、目录大纲、暗色主题、一键导出 HTML/PDF。

纯前端、零依赖、双击即用。专为中文开发者和写作者打造——不联网、不上传、不登录，所有内容都在本地处理。

## ✨ 功能特性

### 编辑与渲染
- **实时预览** —— 输入即渲染，所见即所得，毫秒级响应
- **GFM 语法** —— 标题、粗体/斜体/删除线、链接、图片、代码块、引用、列表、任务列表、表格、分隔线
- **中文排版优化** —— 中英文之间自动加空格，让混排更美观（如「iPhone15 价格 5999 元」）
- **代码语法高亮** —— 支持 JS/TS/Python/JSON/HTML/CSS/Bash/Java/Go/SQL 等 10 种语言，关键字/字符串/注释/数字等 token 着色
- **目录大纲** —— 自动提取标题生成侧边栏目录，点击跳转，可一键显隐

### 主题与导出
- **暗色主题** —— 一键切换亮/暗主题，状态持久化，暗色下高亮配色独立调校
- **一键导出 HTML** —— 导出为独立 HTML 文件（含内联样式），可直接分享或发布
- **PDF 导出** —— 调用浏览器打印对话框，支持 `Ctrl/Cmd+P` 快捷键，导出 PDF 时自动隐藏编辑区
- **复制 HTML** —— 复制渲染后的 HTML 到剪贴板，粘贴到公众号/博客编辑器

### 交互与体验
- **三视图模式** —— 分屏 / 仅编辑 / 仅预览，一键切换
- **拖拽打开** —— 拖拽 `.md/.markdown/.txt` 文件直接载入编辑区
- **打开文件** —— 工具栏按钮选择本地 Markdown 文件
- **拖动调整** —— 拖动中间分隔条调整左右宽度
- **自动保存** —— 内容/视图模式/比例/主题/TOC 显隐均自动保存到 localStorage，刷新不丢失
- **字数统计** —— 实时显示字符数、字数（中文按字、英文按词）、行数
- **隐私优先** —— 纯本地运行，不联网、不上传、不登录
- **XSS 防护** —— 自动转义 HTML 特殊字符，防止注入

## 🚀 使用

### 直接使用
双击 `index.html` 用浏览器打开即可，无需安装、无需服务器、无需联网。

### 快捷键
| 快捷键 | 功能 |
|---|---|
| `Ctrl/Cmd + S` | 导出 HTML 文件 |
| `Ctrl/Cmd + P` | 打印 / 导出 PDF |
| `Tab` | 编辑区内插入两个空格（缩进） |

## 🎯 快捷操作

- **🗂 目录** —— 显示/隐藏左侧目录大纲
- **📋 复制** —— 复制渲染后的 HTML 到剪贴板
- **⬇ 导出** —— 下载为独立 HTML 文件（带内联样式）
- **🖨 PDF** —— 调用浏览器打印对话框
- **🗑 清空** —— 清空编辑区（带二次确认）
- **🌙/☀️ 主题** —— 切换暗色/亮色主题
- **📂 打开** —— 选择本地 `.md` 文件载入
- **⬌ / ✎ / 👁** —— 分屏 / 仅编辑 / 仅预览 三种视图

## 📁 项目结构

```
markdown-preview/
├── index.html          # 页面结构
├── styles.css          # 苹果白风格样式（含暗色主题、代码高亮配色、打印样式）
├── renderer.js         # 渲染逻辑（编辑、预览、TOC、主题、拖拽、PDF 导出）
├── markdown-parser.js  # Markdown 解析核心（零依赖，含 slugify/extractToc/highlightCode）
├── test/
│   └── test.js         # 核心解析逻辑测试（76 用例）
├── .gitignore
├── LICENSE
└── README.md
```

## 🛠 技术栈

- 纯 HTML / CSS / JavaScript（ES5+，兼容性好）
- 零运行时依赖、零构建步骤、零 CDN 引用
- 苹果白高端风格（参考 macOS / iOS 原生设计）
- 自研代码高亮引擎（regex ranges + token 包裹，无 Prism/highlight.js 依赖）

## 🧪 测试

```bash
node test/test.js
```

**76 个用例**覆盖：
- 标题（h1-h6、行内格式、末尾 #）
- 段落、换行
- 行内格式（粗体/斜体/删除线/行内代码）
- 链接、图片、自动链接
- 代码块（语言标记、HTML 转义）
- 引用块、列表（无序/有序/任务）
- 分隔线、表格（含对齐）
- 中文排版优化、XSS 转义、边界情况
- **slugify**（中文保留、标点处理、去重）9 项
- **extractToc**（多级标题、跳过代码块、行内格式剥离）7 项
- **highlightCode**（10 种语言、别名、HTML 转义、字符串内关键字保护）15 项

## 🎨 设计理念

- **苹果白风格** —— 白色/浅灰背景、细腻阴影、系统字体、蓝色 `#007aff` 强调
- **隐私第一** —— 你的文档内容敏感，绝不上传
- **极简极速** —— 双击即用，毫秒级渲染
- **中文优先** —— 中英文自动加空格，界面全中文
- **暗色可选** —— 默认苹果白，暗色作为用户主动切换的可选主题

## 🤝 协作

本项目由 **Trae + MiMo Code** 人机协作打造：MiMo 负责决策与审美评估，Trae 负责架构、代码实现、测试编排。

## ☕ 支持我们

如果这个工具帮到了你，欢迎在爱发电请我们喝杯咖啡：

👉 [https://www.ifdian.net/a/giquwei](https://www.ifdian.net/a/giquwei)

## 🙏 鸣谢

感谢以下朋友的支持（按支持时间排序）：

<!-- 鸣谢名单占位：有了支持者后在这里添加，格式：- [@用户名](主页链接) -->

_暂无，期待第一个支持者的出现。_

## 📄 License

MIT
