# ⚖️ 文本对比管家 · diff-checker

> 苹果白高端风格的文本对比工具：并排 / 内联 / 统一格式三视图，行级 + 字符级双 diff，支持忽略大小写与空白、文件拖放，完全本地运行无上传。

纯前端、零依赖、双击即用。专为需要"找不同"的人打造——开发者比配置/代码、写作者比文档草稿、翻译比译文版本、普通人比合同/邮件修改。所有计算都在本地浏览器完成，不联网、不上传、不登录。

## ✨ 功能特性

- **⚡ 行级 + 字符级双 diff** —— 基于 LCS 动态规划算法，整行标色 + 精确到字符的红绿高亮，让每一处改动无处遁形
- **📊 三种视图一键切换** —— 并排视图（左右对照）、内联视图（顺序展示 +/- 符号）、统一格式（unified diff，可复制粘贴到 Git/邮件）
- **🔧 智能对比选项** —— 忽略大小写、忽略首尾空白、忽略空行，应对各种格式差异场景
- **⊥ 仅显示差异** —— 折叠连续相同行，只聚焦于有差异的部分，快速定位改动
- **⬇ Patch 导出** —— 一键导出 `.patch` 文件，可直接 `git apply` 使用
- **📂 多种输入方式** —— 手动输入、剪贴板粘贴、文件加载、拖放上传（支持任意文本文件，5MB 以内）
- **🧪 内置示例** —— 代码片段、JSON 配置、中文段落、配置文件四组示例，一键体验
- **📋 一键复制** —— 摘要、unified diff 均可一键复制到剪贴板
- **📈 实时统计** —— 新增 / 删除 / 相同行数实时显示，附耗时指标
- **🔄 左右交换** —— 一键交换原文本与新文本，反向查看差异

### PWA 能力

- **离线可用** —— 通过 Service Worker 缓存核心资源，断网后仍可使用
- **安装到桌面** —— 浏览器地址栏安装按钮，一键变为桌面独立应用
- **苹果白高端风格** —— 参考 macOS/iOS 原生设计，白色/浅灰背景、细腻阴影、系统字体、蓝色 `#007aff` 强调

### 键盘快捷键

| 快捷键 | 功能 |
|---|---|
| `Ctrl/Cmd + Enter` | 立即对比 |
| `Ctrl/Cmd + 1` | 并排视图 |
| `Ctrl/Cmd + 2` | 内联视图 |
| `Ctrl/Cmd + 3` | 统一格式视图 |

## 🚀 使用

### 直接使用
双击 `index.html` 用浏览器打开即可，无需安装、无需服务器、无需联网。

### 在线使用
部署到 GitHub Pages 后直接访问（见下方部署说明）。

## 📝 更新日志

### v1.2（2026-07-10）

**新功能：**
- 新增「忽略空行」对比选项 —— 对比时移除空行，空行增删不再计为差异
- 新增「仅显示差异」选项 —— 折叠连续相同行为省略提示，聚焦差异部分（支持并排/内联双视图）
- 新增 Patch 导出 —— 一键导出 `.patch` 文件，可直接 `git apply` 使用
- 新增 `onlydiff=1` URL 参数自动开启仅显示差异模式

**UI 优化：**
- 品牌图标从 emoji 替换为 SVG 矢量图，蓝色背景 + 白色图标更专业
- 卡片阴影增强（shadow-md），输入区与结果区层次更分明
- 页面背景增加微妙渐变（顶部 #efeff4 → 底部 #f5f5f7），消除纯灰平板感
- 选中态选项药丸改为蓝色实心 + 白字 + 阴影，视觉权重更强
- 统计栏各项增加语义色背景药丸（绿/红/灰），信息层级更清晰
- 工具栏增加分隔线，示例标签与操作选项视觉分组
- footer 改为紧凑单行布局，减少底部空白
- diff 高亮背景饱和度提升，绿/红/黄区分度更强
- 行号列加宽至 52px，行号对齐更舒适
- body 改为 flex 列布局，footer 始终贴底，消除中间空白

**测试增强：**
- 测试从 104 项增至 121 项，新增忽略空行场景 7 组 17 个测试用例

### v1.1（2026-07-07）

**严重 bug 修复：**
- 修复 CSS 变量 `--radius` / `--radius-sm` / `--mono` 从未定义却被引用 11 次的问题（导致圆角失效、等宽字体不生效）
- 修复 `charDiff` 处理 emoji 代理对时被拆成两个半位的问题（改用 `Array.from` 按码点切分）
- 修复 `charDiff` 缺少大小保护，超大输入会卡死的问题（500 万 cell 上限退化）
- 修复 `toUnifiedDiff` 上下文行号计算错误 + 多 hunk 分割逻辑问题（引入 `pendingStartA/B` 显式追踪）
- 修复 `toSideBySideBlocks` 空行配对歧义（用 `hasDel/hasAdd` 替代 `dl !== ''` 判断）
- 修复 `navigator.clipboard` 在 `file://` 协议下为 undefined 导致同步 TypeError（降级 `execCommand`）
- 修复 `[hidden]` 属性被 CSS `display:flex/inline-flex` 覆盖导致元素不隐藏的问题

**UI 优化：**
- 统一编辑区与结果区行高（1.6）与字号（13px）
- 优化 empty-row 占位样式（纯灰色块，无重复文字噪音）
- 统计数字用对应语义色（新增绿/删除红/相同灰）加粗
- 加深行号颜色、按钮边框、footer 文字对比度
- textarea 高度自适应内容（160-320px），减少内容少时留白
- footer 增加品牌标识与分隔线，提升收尾层次
- 主题按钮改为矩形圆角与其他按钮风格统一

**测试增强：**
- 测试从 65 项增至 104 项，新增 emoji 代理对、大小保护、unified 上下文行号、多 hunk 分割、空行配对等回归测试

## 🎯 适用场景

| 场景 | 示例 |
|---|---|
| **开发者** | 对比两版配置文件、代码片段、package.json 改动 |
| **写作者** | 对比文档草稿、文章修订前后版本 |
| **翻译** | 对比译文不同版本，定位修改点 |
| **运维** | 对比线上/线下配置文件差异 |
| **普通用户** | 对比合同/邮件修改、找出转载文的篡改 |

## 📁 项目结构

```
diff-checker/
├── index.html          # 页面结构
├── styles.css          # 苹果白高端风格样式
├── app.js              # 交互逻辑（对比、渲染、视图切换、拖放、快捷键）
├── diff-engine.js      # 核心 diff 引擎（LCS 算法，零依赖纯 JS，浏览器/Node 通用）
├── manifest.json       # PWA 应用清单
├── sw.js               # Service Worker（离线缓存）
├── test/
│   └── test.js         # 核心引擎测试（121 项断言全通过）
├── icons/
│   ├── icon-source.png # 图标原图
│   ├── icon-192.png    # PWA 图标
│   ├── icon-512.png    # PWA 图标 / maskable
│   ├── icon-180.png    # apple-touch-icon
│   ├── favicon.ico     # 网站图标（多尺寸）
│   └── make_icons.py   # 图标生成脚本
├── .gitignore
├── LICENSE
└── README.md
```

## 🛠 技术栈

- **HTML5 + CSS3 + 原生 JavaScript** —— 零依赖、零框架、零构建
- **LCS 动态规划算法** —— 行级与字符级 diff 的核心，O(n×m) 时间复杂度
- **PWA** —— Service Worker 离线缓存 + Web App Manifest 桌面安装
- **Python PIL** —— 图标自动生成（PNG + 多尺寸 ICO）
- **苹果白设计系统** —— CSS 变量驱动的统一设计 token

## 🧪 测试

核心 diff 引擎附带完整测试套件：

```bash
node test/test.js
```

覆盖场景：
- 文本切分（CRLF/CR/LF 统一、空字符串、末尾换行、空行保留）
- 字符级 diff（完全相同、空→非空、单字符替换）
- 行级 diff（完全相同、单行替换、纯新增、纯删除）
- compare 主入口（统计、忽略大小写、忽略空白、忽略空行）
- 并排/内联视图块构造
- unified diff 格式生成
- 摘要文本生成
- 大输入保护（500 万 cell 上限退化）
- 真实场景（JSON 配置对比、代码对比）
- 忽略空行场景（纯空行增删、多空行、非空行保留、空白行、组合选项、unified 透传）

## 📦 部署到 GitHub Pages

1. 将整个 `diff-checker/` 目录推送到 GitHub 仓库
2. 仓库 Settings → Pages → Source 选择 `main` 分支
3. 访问 `https://用户名.github.io/仓库名/diff-checker/`

## ☕ 支持我们

如果这个工具帮到了你，欢迎请我们喝杯咖啡 ☕

**[爱发电](https://www.ifdian.net/a/giquwei)** · 你的支持是我们持续打造优质免费工具的动力

## 🙏 鸣谢

感谢以下朋友的支持（按支持时间排序）：

<!-- 鸣谢名单占位 -->

_暂无，期待第一个支持者的出现。_

## 📄 许可证

[MIT License](LICENSE) · 可自由使用、修改、分发

## 🔗 相关项目

文本对比管家是 [youqu 工具集](https://github.com/grrtyre/youqu) 的一员，更多苹果白风格的实用小工具欢迎访问主仓库。
